# ===========================================
# Root .env (Next.js frontend)
# ===========================================
# Copy this file to .env in the project root (same folder as package.json):
#   cp env.example .env
# Then replace the placeholder values below.

# Required: NextAuth base URL (use http://localhost:3000 for local dev)
NEXTAUTH_URL=http://localhost:3000

# Required: Secret for NextAuth JWT. Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=paste-output-of-openssl-rand-base64-32

# Required: PostgreSQL connection (same as in backend/.env)
DATABASE_URL=postgresql://user:password@localhost:5432/dobble

# Optional: FastAPI backend URL (default: http://localhost:8000)
BACKEND_URL=http://localhost:8000
