# ===========================================
# Backend .env (FastAPI)
# ===========================================
# Copy this file to .env inside the backend folder:
#   cp env.example .env
# Then replace the placeholder values. Remove or comment out optional vars you don't use.

# ---------- Required ----------
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/dobble

# JWT signing secret. Generate with: openssl rand -base64 32
JWT_SECRET=paste-output-of-openssl-rand-base64-32

# ---------- LLM (Vertex AI) ----------
# GCP project and region for Vertex AI. Auth via: gcloud auth application-default login
VERTEXAI_PROJECT=your-gcp-project-id
VERTEXAI_LOCATION=us-central1

# ---------- Optional: Google Calendar ----------
# Use either (A) service account or (B) OAuth

# (A) Service account JSON path
# GOOGLE_CREDENTIALS_FILE=path/to/service-account.json

# (B) OAuth (when org blocks service account keys) – run scripts/get_google_refresh_token.py first
# GOOGLE_OAUTH_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=xxx
# GOOGLE_OAUTH_REFRESH_TOKEN=1//xxx

# Calendar to use (default: primary)
# GOOGLE_CALENDAR_ID=primary

# ---------- Optional: SendGrid (email) ----------
# SENDGRID_API_KEY=SG.xxx
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ---------- Optional: Slack (doctor reports) ----------
# Bot token must have chat:write scope. Add scope → Reinstall app → copy new token.
# SLACK_BOT_TOKEN=xoxb-xxx
# SLACK_CHANNEL_ID=C01234ABCD

# ---------- Optional: other ----------
# FRONTEND_ORIGIN=http://localhost:3000
# MCP_SERVER_URL=http://localhost:8000/mcp/sse
# JWT_EXPIRE_MINUTES=10080
